# Python
__pycache__/
*.pyc
.ipynb_checkpoints/
.venv/
venv/

# Generated embeddings (large, regenerated by the pipeline / cached)
data/processed/emb_alephbert.npy
data/processed/emb_alephbert_ids.json

# OS
.DS_Store

# Keep: data/raw/*.json (the pinned corpus snapshot), corpus.jsonl, eval_set.json,
# and results/ outputs are intentionally tracked for reproducibility.
